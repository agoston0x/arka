# Contracts Changelog (changes from Erik's original)

- `script/HelperConfig.s.sol`: `getArbSepoliaConfig()` — derive deployer address from PRIVATE_KEY env var instead of hardcoded `0xBbCbB8...` (allows deploy from any wallet)
